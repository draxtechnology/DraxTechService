<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <add key="Panels" value="U2FsdGVkX1+f3C+TzSBV52yzgMMjNa8=" />

        <!--
    Valid Values =
      Advanced                          U2FsdGVkX1/JJ3QPvEOW5nOGw+zEb4s/
      Email                             U2FsdGVkX1+YN+3YDMt0ebrDTZ2F
      AUTESPA                           U2FsdGVkX191GGJ/bumkNXbg8/TxbEI=                    1070 Channel
      Gent                              U2FsdGVkX1+HX8ee7Qrcq95XoWk=
      Inspire                           U2FsdGVkX1+f3C+TzSBV52yzgMMjNa8=                    1080 Chanel
      MorleyMax                         U2FsdGVkX18lYZGUa4wybYDH/IvY1JtcgA==
	  MorleyZX                          U2FsdGVkX18NyBOg7pRGwII0IHkcqQt3
	  Notifier                          U2FsdGVkX19zEDkZEbF52Y4s3TnjygSE==
	  Pearl                             U2FsdGVkX19VMiux41uUA+jxbmLnn
	  RSM                               U2FsdGVkX18OMDQk8xhCw1xicQ==
	  Syncro                            U2FsdGVkX1/5DsGH2yC+z/jVGTW23A==
      Taktis                            U2FsdGVkX18EWMTvtQVOK6qA+tJkkQ==
      ARM                               U2FsdGVkX18Hlrftm2W91iTLSQ==
      Galaxy                            U2FsdGVkX1/DFpK2vQF3hSdxyyk6iw==

    Galaxy (Honeywell Galaxy Dimension intruder panel, IP-only via its
    Ethernet module - single TCP connection, no serial option): set
    Panels=Galaxy here; everything else lives in GalaxyMan.ini [SetUp]
    (PanelIPAddress, IPPort default 10001, SiteID = the panel Account No.
    menu 56.2.3 which must be non-zero, Passcode default 543210,
    giAmx1Offset). Panel-side prerequisites: SIA level 3 or higher (menu
    56.2.2.1), trigger events 1-19 enabled (18 = zone restorals - without
    it no restores ever arrive), zone Omit attribute set (menu 52) for any
    zone AMX should be able to omit.
    -->


    <add key="Configuration" value="c:\AMX1" />
    <!-- Base folder for temp, log, ini. OPTIONAL since the master-number change:
         when this key is missing or empty the folder defaults to wherever the
         exe is installed (instances 1-4 = the AMX root's NWM1..4 subfolders —
         James's one-tree layout, on whatever drive the site uses), so a stock
         instance config needs only Panels + AmxInstance. Set it explicitly only for non-standard layouts. On
         first run, an empty base folder is seeded with the per-panel ini
         templates the installer ships in the install's ini\ subfolder — the
         service reads inis from the base-folder ROOT; ini\ next to the exe is
         templates only. -->
    <add key="FakeMode" value="0" />
    <!-- 0 = off, 1 = on -->
    <!-- AMX instance / port. A site can run up to four managers in parallel, each
         its own side-by-side Drax360Service install (C:\AMX1 root plus
         C:\AMX1\NWM2..4, its own service name, its own named pipe pair) —
         AmxInstance is the ONE master number all of that keys off (folder,
         service name, pipe pair, port), stamped into each install's config by
         the installer.
           AmxInstance = 1..4  -> port 3090..3093 (AMX1=3090, AMX2=3091, ...)
           AmxPort     = explicit port, overrides AmxInstance's port derivation only
                         (folder/service-name/pipe still follow AmxInstance).
         Omit AmxInstance to default to 1. -->
    <add key="AmxInstance" value="1" />
    <!-- <add key="AmxPort" value="3090" /> -->
    <!-- MQTT mirror for the open head-end / Node-RED PoC. Additive and parallel to
         AMX: when enabled, every normalised event is also published as JSON to a
         local broker. Off by default so a normal install behaves exactly as before.
           MqttEnabled     false = off (default), true = on
           MqttBroker      broker host (default localhost — self-contained, no internet)
           MqttPort        broker TCP port (default 1883)
           MqttTopicPrefix root topic; events -> <prefix>/<panel>/event,
                           log -> <prefix>/<panel>/log, controls in <- <prefix>/<panel>/cmd -->
    <add key="MqttEnabled" value="false" />
    <add key="MqttBroker" value="localhost" />
    <add key="MqttPort" value="1883" />
    <add key="MqttTopicPrefix" value="drax" />
    <!-- Fallback for AMX shutdown. The client exits when AMX sends NWM:END at
         close (legacy NWM parity). If a site's AMX build closes its socket
         without sending END (check the service trace: "Received From AMX:"
         lines followed by "AMX link down"), enable this and the service will
         forward NWM:END to the client itself when the AMX TCP link drops.
         Off by default: a link drop also happens on an AMX crash or restart,
         not just a deliberate close, and closing the operator's client on a
         crash may not be wanted.
           CloseClientOnAmxLoss  false = off (default), true = on -->
    <add key="CloseClientOnAmxLoss" value="false" />
  </appSettings>
</configuration>
